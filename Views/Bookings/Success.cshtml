@model BookingSuccessViewModel

@{
    ViewData["Title"] = "Yêu cầu đặt lịch đã được ghi nhận";
    var customerMessage = Model.CustomerMessage ?? BookingWorkflow.BuildCustomerConfirmationMessage(Model.Booking);
}

<section class="section-block">
    <div class="section-heading">
        <div>
            <span class="section-kicker">Yêu cầu đặt lịch</span>
            <h1>Showroom đã ghi nhận yêu cầu của anh/chị</h1>
        </div>
        <a asp-controller="Cars" asp-action="Index" class="btn btn-outline-light">Xem thêm xe khác</a>
    </div>

    <div class="success-layout">
        <div class="detail-list">
            <div class="detail-list__item">
                <span>Mã yêu cầu</span>
                <strong>@Model.Booking.BookingCode</strong>
            </div>
            <div class="detail-list__item">
                <span>Khách hàng</span>
                <strong>@Model.Booking.CustomerName</strong>
            </div>
            <div class="detail-list__item">
                <span>Số điện thoại</span>
                <strong>@Model.Booking.PhoneNumber</strong>
            </div>
            <div class="detail-list__item">
                <span>Email</span>
                <strong>@Model.Booking.Email</strong>
            </div>
            <div class="detail-list__item">
                <span>Loại dịch vụ</span>
                <strong>@Model.Booking.ServiceType</strong>
            </div>
            <div class="detail-list__item">
                <span>Ngày giờ đề xuất</span>
                <strong>@Model.Booking.AppointmentAt.ToString("dd/MM/yyyy HH:mm")</strong>
            </div>
            <div class="detail-list__item">
                <span>Trạng thái hiện tại</span>
                <strong>@Model.Booking.BookingStatus</strong>
            </div>
        </div>

        <div class="summary-panel">
            <span class="section-kicker">Mẫu xe đã chọn</span>
            <div class="checkout-items mt-3">
                <article class="checkout-item">
                    <img src="@Model.Booking.CarImage" alt="@Model.Booking.CarName" class="checkout-item__image" />
                    <div>
                        <h3>@Model.Booking.CarName</h3>
                        <p class="cart-item-card__price">@Model.Booking.QuotedPrice.ToString("N0") VNĐ</p>
                    </div>
                </article>
            </div>
            <p class="checkout-note">@customerMessage</p>
            @if (!string.IsNullOrWhiteSpace(Model.Booking.AdminNote))
            {
                <div class="checkout-note mt-3">@Model.Booking.AdminNote</div>
            }
            <p class="checkout-note mt-3">Hiện tại lịch này chưa được chốt chính thức. Showroom sẽ thông báo lại sau khi Admin xác nhận.</p>
        </div>
    </div>
</section>

